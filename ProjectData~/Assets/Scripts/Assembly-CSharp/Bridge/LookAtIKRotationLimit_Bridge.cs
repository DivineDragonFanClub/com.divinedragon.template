using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using Combat;

namespace Bridge {
    public sealed class LookAtIKRotationLimit_Bridge : Combat.LookAtIKRotationLimit
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }
    
        // Update is called once per frame
        void Update()
        {
            
        }
    }
}