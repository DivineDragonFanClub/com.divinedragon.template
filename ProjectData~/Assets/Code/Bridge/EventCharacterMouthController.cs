using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using App;

namespace Bridge {
    public class EventCharacterMouthController : App.EventCharacterMouthController
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }
    
        // Update is called once per frame
        void Update()
        {
            
        }
    }
}